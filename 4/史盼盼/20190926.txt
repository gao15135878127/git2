seq2seq模型的简介：
适用范围：翻译软件，就是输入和输出可能长度不同的模型当中
又可以叫做： encoder-decoder模型
流程：1:将输入的数据编码一个上下文的向量c 
2:怎么得到c的  又有三种方法   
①  将编码的最后一个状态赋值给c
② 对最后的一个隐含状态做相应的变换得到c
③ 对所有的隐含状态做变换然后赋值给c
3：拿到之后就对c进行相应的解码  用另一个rnn网络
弊端：
编码器要将整个的序列信息压缩进入一个固定的向量中去，所以不能够完全的表示整个序列的信息，还有一个问题就是我们先前的信息会被之后输入的信息稀释掉，输入序列长度越长，这个现象就是越严重