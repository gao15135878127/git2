日报
RNN是一种特殊的神经网络结构，他是根据“人的认识是基于过往的经验和记忆”这一观点提出的。它与DNN，CNN不同的是：他不仅考虑前一时刻的输入，而且赋予网络对前面的内容的一种“记忆”功能。
RNN之所以称之为循环神经网络，即一个序列当前的输入与前面的输出也有关。具体的表现形式为网络会对前面的消息进行记忆并应用于输出的计算中，即隐藏层之间的节点不再无连接而是有连接的，而且隐藏层的输入不仅包括输入层的输出还包括上一时刻隐藏层的输出。
RNN层级结构：
输入层（Output Layer）
隐藏层（Hidden Layer）
输出层（Input Layer）